import { Event } from "@/core";

export interface DashboardProps {event: Event}

export default function Dashboard(props: DashboardProps) {return (
    <div>
        <span>Dashborad</span>
    </div>
)}